/**
 * Main Scripts
 */

(function($) {

    "use strict";

    /**
     * Utilities
     */

    // Safe logging
    function log(msg) {
        if (window.console) {
            console.log(msg);
        }
    }

})(jQuery);